### gox
> 记录学习go语言过程中写的零碎/测试代码

### 目录说明
1. `apps` 目录下子目录可通过 ``go install` 进行安装使用
   1. `ghello` 可通过 `go install github.com/ynwcel/gox/apps/ghello@latest` 安装
2. `pkgs` 开头的子目录为单独的模块目录
   1. `xnum` 可通过  `go get github.com/ynwcel/gox/pkgs/xnum` 下载并使用